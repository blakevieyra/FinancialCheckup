# Production API for static web + mobile builds (no trailing slash)
# Example Render API host:
# VITE_API_BASE_URL=https://financialcheckup-api.onrender.com

# Local dev: proxy to API (see vite.config.js)
# VITE_PROXY_TARGET=http://localhost:3001
